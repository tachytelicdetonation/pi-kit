/** Shown in collapsed tool results; Pi keybinding is still `app.tools.expand` (Ctrl+O). */
export declare const COLLAPSED_EXPAND_LABEL = "ctrl+o to expand";
/** Collapsed hint line (no extra blank lines above/below). */
export declare function collapsedExpandBlock(countLabel: string): string;
/** When line count / metrics already appear on the header line (bash). */
export declare function collapsedExpandFooter(): string;
//# sourceMappingURL=collapsed-hint.d.ts.map