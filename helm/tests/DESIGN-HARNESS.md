# Design harness

**Purpose:** Durable map from design guarantees to their arbitrating tests. Update it whenever tests or design documents are added or removed.

**Provenance:** Derived from `design_handoff_pi_tui/` and `helm/README.md`, 2026-07-19.

**Protected tier:** `conformance-v2/**` is the adjudicated arbiter tier; never edit it without gate adjudication.

| ID | guarantee | design source | status | tests |
| --- | --- | --- | --- | --- |
| D1 | Only final screens 3a, 4a, 6a–6c, 7a–7d, and 8a–8b are authoritative. User-accepted low-impact gap: no global rejection test for superseded screens. | `design_handoff_pi_tui/README.md:9-11` | ACCEPTED-GAP | — |
| D2 | Every screen has `pi · <context>` at left, status/counts at right, then a full-width divider. | `design_handoff_pi_tui/README.md:26-27` | COVERED | `chrome.test.ts:header places 'pi · <context>' on the left`<br>`chrome.test.ts:divider is exactly width filled cells` |
| D3 | Left is names/verbs, middle is description, and right is status/proof/timing only. Missing: mission control intentionally renders the numbered action key in the right column. | `design_handoff_pi_tui/README.md:28` | PRODUCT-GAP | — |
| D4 | Responsive truncation never separates a hotkey from its label. | `design_handoff_pi_tui/README.md:29` | COVERED | `conformance-v2/rendering-invariants.runnable.test.ts:Rendering contract: closeout hotkey-label pairs drop atomically at narrow widths`<br>`conformance-v2/rendering-invariants.runnable.test.ts:Rendering contract: loop-builder hotkey-label pairs drop atomically at narrow widths` |
| D5 | Purple model tags appear only when a lane differs from the main model. | `design_handoff_pi_tui/README.md:30` | COVERED | `conformance-v2/rendering-invariants.runnable.test.ts:Rendering contract: a model tag matching the default is suppressed; a differing tag is shown` |
| D6 | Screens use the specified semantic glyph vocabulary. User-accepted low-impact gap: no full-inventory substitution test covers every screen. | `design_handoff_pi_tui/README.md:24` | ACCEPTED-GAP | `mission-control.test.ts:tree glyphs ├ and └ render under goals`<br>`drill-in.test.ts:race-to-green strips render ▊ ticks in red/green with a right status`<br>`digest.test.ts:the four line types appear IN ORDER with the correct glyphs and colors` |
| D7 | The provider budget bar uses the specified fixed order, allocation formula, cells, spent capacity, and average. | `design_handoff_pi_tui/README.md:34-38` | COVERED | `footer.test.ts:computeStackedBar splits [90,100,60] into 9/10/6 cells at 83%`<br>`conformance-v2/footer.runnable.test.ts:Footer row: canonical provider vectors produce a clamped fixed-width budget bar`<br>`conformance-v2/footer.runnable.test.ts:Footer row EXTRA invariant: bar allocation never exceeds its width for arbitrary remaining vectors` |
| D8 | Missing or corrupt usage never masquerades as 0% or 100%. | `helm/README.md:60-66` | COVERED | `usage/ui.test.ts:all providers unknown renders '--', not a misleading 0%`<br>`usage/ui.test.ts:a corrupt (non-finite) provider bucket is treated as unknown, not 100%`<br>`conformance-v2/proposed-extra.test.ts:PROPOSED usage: an empty provider list is explicit, bounded, and never masquerades as 0%` |
| D9 | The session footer context meter is eight cells and reports `ctx NN%`. | `design_handoff_pi_tui/README.md:38` | COVERED | `footer.test.ts:computeCtxMeter fills round(percent/100*8) cells`<br>`conformance-v2/footer.runnable.test.ts:Footer row: the context meter remains an eight-cell bar throughout 70-89 columns` |
| D10 | Footer content degrades at the 110, 90, and 70-column thresholds. | `design_handoff_pi_tui/README.md:39` | COVERED | `footer.test.ts:footer degrades cwd below 110 cols, drops middle meter below 70`<br>`conformance-v2/footer.runnable.test.ts:Footer row: responsive boundaries keep the wider form at 110, 90, and 70` |
| D11 | Session and fleet footers remain distinct: context/session cost versus burn rate/daily spend. | `design_handoff_pi_tui/README.md:40`; `helm/README.md:17-19` | COVERED | `footer.test.ts:session variant renders ctx and $cost`<br>`footer.test.ts:fleet variant shows burn, tok/min and today`<br>`conformance-v2/footer.runnable.test.ts:Footer row: session and fleet variants expose their contracted strings` |
| D12 | The footer is always visible and pinned last. | `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b); `helm/README.md:83-85` | COVERED | `app.test.ts:the fleet footer is the last line`<br>`popover.test.ts:ctrl+u opening the popover keeps the exact-rows + footer-last guarantees`<br>`phase5-global.test.ts:a rows change reflows every screen to the new exact line count` |
| D13 | Session activity is semantic `context`, `parallel`, `edit`, and `verify`, not raw tool logs. | `design_handoff_pi_tui/README.md:43-45` | COVERED | `session.test.ts:all four verbs render in the fixed 9-char left column` |
| D14 | Consecutive reads, greps, and read-only shell calls collapse into one context row. | `design_handoff_pi_tui/README.md:45` | COVERED | `conformance-v2/session.runnable.test.ts:Session row: consecutive reads, greps, and read-only bash collapse to exactly one context row`<br>`conformance-v2/proposed-extra.test.ts:PROPOSED history: one thousand consecutive context entries still adapt to one bounded row` |
| D15 | Concurrent tool calls become one parallel row with joined summaries. | `design_handoff_pi_tui/README.md:45` | COVERED | `conformance-v2/session.runnable.test.ts:Session row: concurrent tool calls become one parallel row with joined summaries` |
| D16 | Edit rows show the real path and measured `+N −N`. | `design_handoff_pi_tui/README.md:45-46` | COVERED | `session.test.ts:edit rows show +N −N (added success / removed error)`<br>`conformance-v2/session.runnable.test.ts:Session row: edit adaptation preserves the real path and measured +N −N` |
| D17 | Expanded edits show at most three numbered lines and the exact remainder. | `design_handoff_pi_tui/README.md:46` | COVERED | `session.test.ts:expanding an edit shows the 3-line peek + '… N more · d full diff'`<br>`conformance-v2/session.runnable.test.ts:Session row: a 500-line edit renders at most three numbered content lines plus the exact remainder` |
| D18 | Tests, builds, and lint classify as `verify`; read-only shell does not. | `design_handoff_pi_tui/README.md:45` | COVERED | `session.test.ts:verify is first-class (its own verb, never shown as bash)`<br>`conformance-v2/session.runnable.test.ts:Session row: only tests, builds, and lint classify as verify` |
| D19 | Receipts preserve parsed build, lint, and real test evidence instead of generic success. | `design_handoff_pi_tui/README.md:48` | COVERED | `conformance-v2/session.runnable.test.ts:Session row: receipt rendering keeps build, lint, and the real test total distinct`<br>`conformance-v2/session.runnable.test.ts:Session row: adapter receipts use parsed build/lint/test evidence rather than a generic success` |
| D20 | A session turn ends with a claim and one-key review/merge actions. | `design_handoff_pi_tui/README.md:47` | COVERED | `session.test.ts:the turn ends with a claim + 'review diff d · merge m' actions` |
| D21 | Intake launches no agents before explicit `g`, which stays disabled while questions remain. | `design_handoff_pi_tui/README.md:51-52`; `helm/README.md:28` | COVERED | `intake.test.ts:\`g\` is a NO-OP while pi has open questions (stays on intake, dim)`<br>`conformance-v2/search-popover-intake.runnable.test.ts:Intake row: vague intent derives only un-inferable questions and no workflow starts before g`<br>`persistence.test.ts:intake planning happens before spawn and re-plans until all structured questions are answered` |
| D22 | Intake re-derives its structured plan after clarification without launching work. | `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b) | COVERED | `conformance-v2/search-popover-intake.runnable.test.ts:Intake row: answering clarification re-derives the structured plan before launch`<br>`persistence.test.ts:intake planning happens before spawn and re-plans until all structured questions are answered` |
| D23 | Intake renders one line per workflow, estimates/escalation rule, and exact `g/e/x` actions. | `design_handoff_pi_tui/README.md:52` | COVERED | `intake.test.ts:intake shows the goal request, NUMBERED questions, and the plan block`<br>`functional-actions.test.ts:intake plan editing uses e, j/k, enter, then persists the rewritten line` |
| D24 | Mission control contains needs-you, goals, and loops in fixed order and suppresses tool/subagent detail. | `design_handoff_pi_tui/README.md:54-59` | COVERED (residual) | strata order: `mission-control.test.ts:the three strata appear in fixed order: needs-you, goals, loops`. Suppression clause has NO structural enforcement: `Workflow.summary`/`Loop.pipelineSummary` (types.ts:42,56) are free-form strings rendered verbatim on home — the guarantee rests on the data layer writing semantic summaries; no test seam exists today. |
| D25 | The needs-you header count equals the data-backed escalation count. | `design_handoff_pi_tui/README.md:56`; `helm/README.md:60-66` | COVERED | `mission-control.test.ts:the needs-you header count matches the escalation count`<br>`selectors.test.ts:needsYouCount counts escalations` |
| D26 | Home shows top-level workflows with tree glyphs and never subagent trees. | `design_handoff_pi_tui/README.md:57` | COVERED (residual) | `selectors.test.ts:workflowsForGoal returns only that goal's top-level workflows`<br>`mission-control.test.ts:tree glyphs ├ and └ render under goals`. Same residual as D24: free-form summary strings on home are unenforced. |
| D27 | Lists re-sort only on state changes, not progress ticks. | `design_handoff_pi_tui/README.md:60,89`; `helm/README.md:91-92` | COVERED | `store.test.ts:sort order is stable across snapshots when nothing changed`<br>`store.test.ts:paused loops sink below active ones, order otherwise preserved`<br>`store.test.ts:escalations are stored oldest-blocked first, ties broken by id` |
| D28 | A new needs-you item rings BEL; resolution and unchanged state do not. | `design_handoff_pi_tui/README.md:60`; `helm/README.md:74` | COVERED | `phase5-global.test.ts:the bell rings when a NEW escalation is added (mockable, no timers)`<br>`phase5-global.test.ts:the bell does NOT ring when an escalation is DECIDED (the count drops)`<br>`phase5-global.test.ts:no onBell sink is safe (never throws when a new escalation is added)` |
| D29 | An unfocused needs-you item produces a deduplicated OS notification after 60 seconds. Missing: no focus detection, 60-second notification logic, OS notification seam, or deduplication. | `design_handoff_pi_tui/README.md:60`; `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b) | PRODUCT-GAP | — |
| D30 | Workflow drill-in uses “lanes,” never “loops,” for internal pipelines. | `design_handoff_pi_tui/README.md:62-63` | COVERED | `drill-in.test.ts:the word 'loops' NEVER appears (internal pipelines are 'lanes')` |
| D31 | Drill-in burn-down shows count, original total, burn rate, and ETA. | `design_handoff_pi_tui/README.md:64` | COVERED | `drill-in.test.ts:the burn-down line shows the count, unit and all four labels` |
| D32 | Worktree rows use correctly colored fix→review→apply chips. | `design_handoff_pi_tui/README.md:65` | COVERED | `drill-in.test.ts:worktree rows show fix→review→apply chips with the correct stage colors` |
| D33 | Race-to-green strips report real history and are omitted when history is absent. | `design_handoff_pi_tui/README.md:66` | COVERED | `drill-in.test.ts:race-to-green strips render ▊ ticks in red/green with a right status`<br>`drill-in.test.ts:the pkg/http worktree (no ticks) gets no race strip` |
| D34 | Burn-down/ETA updates once per minute and chips animate only on transitions. Missing: no clock/cadence cache or transition-only animation seam exists. | `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b) | PRODUCT-GAP | — |
| D35 | Enter on a workflow-drill-in worktree opens its session. | `design_handoff_pi_tui/README.md:67` | COVERED | `app.test.ts:enter on a 6c worktree row pushes the 4a session` |
| D36 | Loop builder exposes trigger, steps, skips, and guardrails including model. | `design_handoff_pi_tui/README.md:69-70` | COVERED | `loop-builder.test.ts:the builder structures the loop into trigger / steps / skips / guardrails`<br>`loop-builder.test.ts:a per-step guardrail model is tagged PURPLE`<br>`loop-builder.test.ts:guardrails per-step model survives narrow widths (wrapped, not truncated)` |
| D37 | Scheduling requires a passed supervised trial; failure reopens and never auto-retries. | `design_handoff_pi_tui/README.md:70` | COVERED | `conformance-v2/loops-pause.runnable.test.ts:Loops row: scheduling is rejected without a passed supervised verdict`<br>`conformance-v2/loops-pause.runnable.test.ts:Loops row: machine-readable failed verdict overrides a technically completed trial run`<br>`conformance-v2/loops-pause.runnable.test.ts:Loops row: failed trial persists no trialPassed gate and is invoked only once` |
| D38 | Editing a scheduled loop cannot mutate its armed definition before retrial. | `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b); `helm/README.md:67-69` | COVERED | `conformance-v2/loops-pause.contract.test.ts:Loops contract row: editing a scheduled loop preserves activeDefinition and resets the draft gate`<br>`conformance-v2/loops-pause.contract.test.ts:Loops contract row: an armed firing reads immutable activeDefinition, never the pending draft` |
| D39 | A home loop opens loop drill-in, then builder; history is scoped, newest-first, and truthful. | `helm/README.md:53-56`; `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8a) | COVERED | `conformance-v2/loop-drillin.test.ts:Loop drill-in N1: enter on a home loop row opens drill-in, not the builder`<br>`conformance-v2/loop-drillin.test.ts:Loop drill-in R2: runs render newest-first with success, failure, and distinct trial markers`<br>`conformance-v2/loop-drillin.test.ts:Loop drill-in D2: a never-fired real loop has no fabricated history and shows no runs yet` |
| D40 | Escalations show source/age/idle cost, one-sentence problem, bounded evidence, numbered options, and one green recommendation. | `design_handoff_pi_tui/README.md:72-73` | COVERED | `escalation.test.ts:the body is a ONE-sentence problem statement (wrapped, single sentence)`<br>`escalation.test.ts:the evidence block is present and SMALL (never the full file)`<br>`escalation.test.ts:options are numbered with EXACTLY ONE recommended highlighted green`<br>`escalation.test.ts:the app header shows 'blocked N min' + the idle note for the card` |
| D41 | Escalation follow-up persists on the active card. | `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b) | COVERED | `conformance-v2/escalation.contract.test.ts:Escalation contract row: ? appends a persisted follow-up and leaves the card active`<br>`persistence.test.ts:follow-up questions and answers persist while the escalation stays active` |
| D42 | Decisions become precedents; identical decision conflicts auto-resolve with an audit note. | `design_handoff_pi_tui/README.md:73,93` | COVERED | `conformance-v2/escalation.contract.test.ts:Escalation contract row: identical decision conflict auto-resolves and writes a precedent note`<br>`persistence.test.ts:decision signatures auto-resolve stably, declined precedents persist, and permission prompts never auto-resolve` |
| D43 | Permission-class decisions never auto-resolve. | `helm/README.md:71-74` | COVERED | `conformance-v2/escalation.contract.test.ts:Escalation contract row: permission-class escalation never auto-resolves from stored precedent`<br>`persistence.test.ts:decision signatures auto-resolve stably, declined precedents persist, and permission prompts never auto-resolve` |
| D44 | Digest appears only after strictly more than 30 minutes away. | `design_handoff_pi_tui/README.md:75-76` | COVERED | `conformance-v2/digest-data.runnable.test.ts:Digest row: launch gate is false at exactly 30 minutes and true one millisecond later`<br>`triage.test.ts:initial stack is [home, digest] when shouldShowDigest() is true`<br>`triage.test.ts:initial stack is [home] when shouldShowDigest() is false (the default)` |
| D45 | Digest renders its four line types in strict order. | `design_handoff_pi_tui/README.md:76` | COVERED | `digest.test.ts:the four line types appear IN ORDER with the correct glyphs and colors`<br>`conformance-v2/digest-data.runnable.test.ts:Digest row: all four line types render in strict order` |
| D46 | Digest never fabricates events, spend, or provider drain. | `design_handoff_pi_tui/README.md:76,95`; `helm/README.md:60-66` | COVERED | `conformance-v2/digest-data.runnable.test.ts:Digest row: starting a goal emits no prOpened journal event`<br>`conformance-v2/digest-data.runnable.test.ts:Digest row: spend and provider drain are deltas from the persisted last-seen usage snapshot`<br>`conformance-v2/digest-data.runnable.test.ts:Digest row: a journal without a revert receipt never fabricates selfCaughtRevert`<br>`conformance-v2/digest.contract.test.ts:Digest contract row: caught failure persists as selfCaughtPaused and renders self-caught · paused` |
| D47 | Digest footer exposes decisions, home, log, and provider drain. | `design_handoff_pi_tui/README.md:76` | COVERED | `digest.test.ts:the footer line has the three key hints + the per-provider quota drain`<br>`digest.test.ts:'d' on the digest jumps into the decision queue (first escalation card)`<br>`digest.test.ts:'enter' on the digest goes to mission control (home)`<br>`digest.test.ts:'l' opens the full activity log` |
| D48 | Closeout receipt contains measured completion, change totals, and actual-versus-estimated cost. | `design_handoff_pi_tui/README.md:78-79` | COVERED | `closeout.test.ts:the big receipt shows the count, added/removed, and actual-vs-estimate cost`<br>`conformance-v2/closeout.runnable.test.ts:Closeout row: a completed goal exposes concrete measured metrics, not placeholder prose` |
| D49 | Closeout includes attention, interventions, and overrun reasons. | `design_handoff_pi_tui/README.md:79` | COVERED | `closeout.test.ts:the three summary rows render (your time / interventions / overrun why)`<br>`conformance-v2/closeout.runnable.test.ts:Closeout row: a completed goal exposes concrete measured metrics, not placeholder prose` |
| D50 | Declined and applied precedents are durably suppressed. | `design_handoff_pi_tui/README.md:79`; `design_handoff_pi_tui/Usage Bar Explorations.dc.html` (8b) | COVERED | `conformance-v2/closeout.contract.test.ts:Closeout contract row: declining a precedent persists and suppresses it after relaunch`<br>`conformance-v2/closeout.contract.test.ts:Closeout contract row: applied precedent state survives relaunch`<br>`precedent.test.ts:DECLINED precedents are never auto-applied` |
| D51 | Enter descends, escape ascends, and navigation has no dead ends; escape at home never quits. | `design_handoff_pi_tui/README.md:81-82`; `helm/README.md:17-19` | COVERED | `app.test.ts:esc ascends: session → drill-in → home, then stays at home`<br>`router.test.ts:pop repeatedly bottoms out at [home] and never empties`<br>`conformance-v2/keys.runnable.test.ts:Keys row: escape at empty home never exits, even repeatedly` |
| D52 | Bound keys act only in command mode; printable input types to Pi; `q` types and `ctrl+q` quits. | `design_handoff_pi_tui/README.md:84-85`; `helm/README.md:35-47` | COVERED | `conformance-v2/keys.runnable.test.ts:Keys row: q is unbound and types into an empty prompt; ctrl+q exits`<br>`conformance-v2/proposed-extra.test.ts:PROPOSED input: unicode prompt text survives key dispatch and rendering intact`<br>`conformance-v2/proposed-extra.test.ts:PROPOSED gap: a bound action remains literal after prompt focus (Gmail rule precedence)` |
| D53 | `j/k`, arrows, Enter, and `1-9` operate on selectable or numbered items globally. Missing: `j/k` types into search on some paths, and Enter is a no-op on selectable session rows. | `design_handoff_pi_tui/README.md:85` | PRODUCT-GAP | — |
| D54 | Tab globally walks and wraps needs-you items from every screen. | `design_handoff_pi_tui/README.md:85` | COVERED | `triage.test.ts:tab cycles through EVERY escalation in the queue`<br>`triage.test.ts:tab from a DEEP screen (session) resets to [home, escalation] — never grows the stack`<br>`conformance-v2/keys.runnable.test.ts:Keys row: tab jumps to needs-you from ${scenario.name}` |
| D55 | Search is permanent and makes each autonomous event reachable through `/`, then Enter. | `design_handoff_pi_tui/README.md:85,89`; `helm/README.md:31,40` | COVERED | `conformance-v2/search-popover-intake.runnable.test.ts:Search row: archived goals, decisions, loop runs, and PR events are all findable`<br>`design-harness/h4-audit-search.test.ts:H4: / search reaches persisted merged work detail`<br>`design-harness/h4-audit-search.test.ts:H4: / search reaches persisted self-caught pause detail` |
| D56 | Usage popover is a truthful overlay; any key closes it first without changing navigation. | `design_handoff_pi_tui/README.md:85`; `helm/README.md:41-43` | COVERED | `popover.test.ts:ANY key closes the popover FIRST and is consumed (navigation undisturbed)`<br>`popover.test.ts:the popover does not change the nav stack (esc still one hop to home from deep)`<br>`conformance-v2/search-popover-intake.runnable.test.ts:Popover row: real today/week spend and per-goal costs render without seed placeholders` |
| D57 | Pause-all is durable and scope-preserving; repeat resumes, deadlines shift, and the footer turns yellow. | `design_handoff_pi_tui/README.md:85`; `helm/README.md:44-45` | COVERED | `conformance-v2/loops-pause.runnable.test.ts:Pause row: global pause survives a fresh data-source instance`<br>`conformance-v2/loops-pause.runnable.test.ts:Pause row: lanes paused before pause-all remain paused after resume-all`<br>`conformance-v2/loops-pause.runnable.test.ts:Pause row: loop deadline shifts by the full paused duration`<br>`conformance-v2/footer.runnable.test.ts:Footer row: paused visible text uses warning yellow exclusively` |
| D58 | Per-screen help appears on the prompt line, is screen-specific, and clears on navigation. | `design_handoff_pi_tui/README.md:85`; `helm/README.md:46` | COVERED | `phase5-global.test.ts:\`?\` toggles a per-screen key-help hint on the prompt line (no literal '?')`<br>`phase5-global.test.ts:the help hint is per-screen and clears on navigation` |
| D59 | Every destructive command requires confirmation; pause and other non-destructive commands do not. | `design_handoff_pi_tui/README.md:89`; `helm/README.md:71-74` | COVERED | `design-harness/h1-confirmation-matrix.test.ts:H1: the known safety classification exactly covers the HelmCommand registry`<br>`design-harness/h1-confirmation-matrix.test.ts:H1: a destructive numbered escalation option requires confirmation before durable resolution`<br>`design-harness/h1-confirmation-matrix.test.ts:H1: global pause and resume take effect immediately without a confirmation prompt` |
| D60 | Rendering and persistence fail safely across dimensions, color modes, project storage, and renderer exceptions. User-accepted low-impact gap: no canonical exact-color token-table test. | `helm/README.md:60-69,83-90`; `design_handoff_pi_tui/README.md:14-24` | ACCEPTED-GAP | `app.test.ts:tiny and zero terminals never throw and never overflow`<br>`phase5-global.test.ts:in 256color mode NO truecolor (\x1b[38;2) escape leaks on any screen`<br>`persistence.test.ts:goal intent and scheduled-loop definitions survive a fresh data-source instance`<br>`design-harness/h2-render-cleanup.test.ts:H2: a render exception releases the full-screen command and restores the host usage footer` |

## How to use

- Adding a feature: find its guarantee row(s), extend the tests, and update the row.
- Adding a design document: extend the registry.
