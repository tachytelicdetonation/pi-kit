{
  "name": "pi-claude-cmux",
  "version": "0.1.0",
  "private": true,
  "description": "Event-driven orchestration of interactive Claude Code sessions through cmux",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi",
    "claude-code",
    "cmux",
    "orchestration"
  ],
  "pi": {
    "extensions": [
      "./extensions/claude-cmux.ts"
    ]
  },
  "files": [
    "extensions/",
    "src/",
    "README.md"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "npm run check && tsx --test tests/**/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "0.80.10",
    "@earendil-works/pi-coding-agent": "0.80.10",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typebox": "^1.0.55",
    "typescript": "^5.9.0"
  }
}
